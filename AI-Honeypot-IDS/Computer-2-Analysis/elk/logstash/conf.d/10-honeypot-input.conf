# =============================================================================
# Logstash pipeline — 10-honeypot-input.conf
# Beats/Lumberjack v2 input from Filebeat on Computer 1
# =============================================================================
input {
  beats {
    port => 5044
    # Lab only — no TLS
    ssl => false
    # Tolerate brief disconnects
    client_inactivity_timeout => 300
  }
}
