# This file exists so the logs/ directory is tracked by git even when empty.
# Actual log files are written into a Docker named volume (honeypot-logs)
# shared by every container; this host-side folder is for ad-hoc copies and
# debugging.
